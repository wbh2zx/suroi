# Suroi 开发经验

## 新增物品（武器/投掷物/装备）

### 必改文件

| 步骤 | 文件 | 说明 |
|------|------|------|
| 定义 | `common/src/definitions/items/<类型>.ts` | 物品参数（伤害、范围、动画等） |
| 爆炸 | `common/src/definitions/explosions.ts` | 如果是投掷物，需要爆炸定义 |
| 掉落 | `server/src/data/lootTables.ts` | 刷新率、数量 |
| 容量 | `common/src/definitions/items/backpacks.ts` | **每个等级背包都要加**，否则捡不起来 |
| 协议 | `common/src/constants.ts` | `protocolVersion` +1 |

### 必复制贴图

| 文件 | 位置 | 作用 |
|------|------|------|
| `proj_<id>_pin.svg` | `client/public/img/game/shared/projectiles/` | 手部动画 |
| `proj_<id>.svg` | 同上 | 手部动画 |
| `proj_<id>_lever.svg` | 同上 | 手部动画 |
| `proj_<id>_nopin.svg` | 同上 | 手部动画 |
| `<idString>.svg` | `client/public/img/game/shared/weapons/` | **地面战利品图标** |
| `<id>_killfeed.svg` | `client/public/img/killfeed/` | 击杀提示 |
| `<id>_explosion_killfeed.svg` | `client/public/img/killfeed/` | 爆炸击杀提示 |

贴图文件缺失 → 游戏显示紫黑方块。

### 常见报错

| 报错 | 原因 | 解决 |
|------|------|------|
| 空间不足 | `backpacks.ts` 没加该物品容量 | 每个背包等级加一条 |
| 紫黑方块 | 对应贴图文件不存在 | 复制现有贴图改名 |

## 贴图缺失排查

`client/public/img/` 下找不到对应 svg 文件就复制同类物品的改个名。可以先不画新图，用同类物品贴图顶替。
